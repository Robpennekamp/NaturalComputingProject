import type { IDataObject, IExecuteFunctions } from 'n8n-workflow';
export interface RundeckCredentials {
    url: string;
    token: string;
}
export declare class RundeckApi {
    private credentials?;
    private executeFunctions;
    constructor(executeFunctions: IExecuteFunctions);
    protected request(method: string, endpoint: string, body: IDataObject, query: object): Promise<any>;
    init(): Promise<void>;
    executeJob(jobId: string, args: IDataObject[]): Promise<IDataObject>;
    getJobMetadata(jobId: string): Promise<IDataObject>;
}
