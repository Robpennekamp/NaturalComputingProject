export interface ITask {
    id?: string;
    title?: string;
    status?: TaskStatus;
    flag?: boolean;
    owner?: string;
    description?: string;
    startDate?: Date;
    endDate?: Date;
    createdBy?: string;
    createdAt?: Date;
    updatedBy?: string;
    upadtedAt?: Date;
}
export declare const enum TaskStatus {
    WAITING = "Waiting",
    INPROGRESS = "InProgress",
    COMPLETED = "Completed",
    CANCEL = "Cancel"
}
