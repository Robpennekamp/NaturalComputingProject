import type { EntityManager, FindOptionsWhere } from 'typeorm';
import { Role } from '../databases/entities/Role';
import { SharedWorkflowRepository } from '../databases/repositories';
export declare class RoleService {
    private sharedWorkflowRepository;
    constructor(sharedWorkflowRepository: SharedWorkflowRepository);
    static trxGet(transaction: EntityManager, role: FindOptionsWhere<Role>): Promise<Role | null>;
    getUserRoleForWorkflow(userId: string, workflowId: string): Promise<Role | undefined>;
}
