import { VersionControlPreferences } from './types/versionControlPreferences';
import type { ValidationError } from 'class-validator';
export declare class VersionControlService {
    private _versionControlPreferences;
    init(): Promise<void>;
    get versionControlPreferences(): VersionControlPreferences;
    set versionControlPreferences(preferences: Partial<VersionControlPreferences>);
    generateAndSaveKeyPair(): Promise<{
        privateKey: string;
        publicKey: string;
    }>;
    validateVersionControlPreferences(preferences: Partial<VersionControlPreferences>): Promise<ValidationError[]>;
    setPreferences(preferences: Partial<VersionControlPreferences>, saveToDb?: boolean): Promise<VersionControlPreferences>;
    loadFromDbAndApplyVersionControlPreferences(): Promise<VersionControlPreferences | undefined>;
}
