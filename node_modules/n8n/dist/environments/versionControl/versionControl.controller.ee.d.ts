import { VersionControlService } from './versionControl.service.ee';
import { VersionControlRequest } from './types/requests';
import type { VersionControlPreferences } from './types/versionControlPreferences';
export declare class VersionControlController {
    private versionControlService;
    constructor(versionControlService: VersionControlService);
    getPreferences(): Promise<VersionControlPreferences>;
    setPreferences(req: VersionControlRequest.UpdatePreferences): Promise<VersionControlPreferences>;
    generateKeyPair(): Promise<{
        privateKey: string;
        publicKey: string;
    }>;
}
