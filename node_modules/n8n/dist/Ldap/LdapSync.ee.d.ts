import type { LdapService } from './LdapService.ee';
import type { LdapConfig } from './types';
import type { RunningMode } from '../databases/entities/AuthProviderSyncHistory';
export declare class LdapSync {
    private intervalId;
    private _config;
    private _ldapService;
    set config(config: LdapConfig);
    set ldapService(service: LdapService);
    scheduleRun(): void;
    run(mode: RunningMode): Promise<void>;
    stop(): void;
    private getUsersToProcess;
    private getUsersToCreate;
    private getUsersToUpdate;
    private getUsersToDisable;
}
