import { LdapService } from '../Ldap/LdapService.ee';
import { LdapSync } from '../Ldap/LdapSync.ee';
import { LdapConfiguration } from '../Ldap/types';
import { InternalHooks } from '../InternalHooks';
export declare class LdapController {
    private ldapService;
    private ldapSync;
    private internalHooks;
    constructor(ldapService: LdapService, ldapSync: LdapSync, internalHooks: InternalHooks);
    getConfig(): Promise<import("../Ldap/types").LdapConfig>;
    testConnection(): Promise<void>;
    updateConfig(req: LdapConfiguration.Update): Promise<import("../Ldap/types").LdapConfig>;
    getLdapSync(req: LdapConfiguration.GetSync): Promise<import("../databases/entities/AuthProviderSyncHistory").AuthProviderSyncHistory[]>;
    syncLdap(req: LdapConfiguration.Sync): Promise<void>;
}
