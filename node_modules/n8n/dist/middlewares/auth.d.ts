import type { Application } from 'express';
import type { UserRepository } from '../databases/repositories';
export declare const setupAuthMiddlewares: (app: Application, ignoredEndpoints: Readonly<string[]>, restEndpoint: string, userRepository: UserRepository) => void;
