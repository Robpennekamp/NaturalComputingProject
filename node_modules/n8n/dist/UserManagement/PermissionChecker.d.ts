import type { Workflow } from 'n8n-workflow';
export declare class PermissionChecker {
    static check(workflow: Workflow, userId: string): Promise<void>;
    static checkSubworkflowExecutePolicy(subworkflow: Workflow, userId: string, parentWorkflowId?: string): Promise<void>;
    private static mapCredIdsToNodes;
}
